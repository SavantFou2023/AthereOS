# AetherOS Agent Notes

- Keep frontend framework-free (HTML/CSS/vanilla JS).
- Keep backend lightweight (FastAPI + WebSocket).
- Do not add hidden assistant actions; every simulated step must be visible in UI.
- Maintain a clean modular split between planning and execution logic.
- Preserve Windows-friendly run instructions.
